#EVENT {MINUTE}
{
  #IF {@fEval{%5 % 5} == 0}
  {
    event_raise {e_5_mins} {%5};
  }; 

  #IF {@fEval{%5 % 15} == 0}
  {
    event_raise {e_15_mins} {%5};
  };

  #IF {@fEval{%5 % 30} == 0}
  {
    event_raise {e_30_mins} {%5};
  };
};
